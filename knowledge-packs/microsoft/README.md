# Official Microsoft packs are not installed

No production Microsoft recommendation values are bundled. The application includes validation and import workflows, but an official pack may be added only after the exact product, baseline version, setting identifiers, values, platform/scope applicability, official source URLs, verification date, and hashes are established.

