# Knowledge packs

Knowledge packs are versioned, hash-bound JSON inputs to the deterministic comparison engine. A pack must pass schema, provenance, identifier, alias, value, evidence-domain, and hash validation before activation.

Only `synthetic.test-baseline` ships with the repository. It is visibly fictional and contains no Microsoft recommendation. Directories under `microsoft/` intentionally contain documentation only until exact official source content is independently verified.

