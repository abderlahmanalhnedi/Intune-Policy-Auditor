# Synthetic Samples

Every file in this directory uses fictional setting identifiers and test data. The samples contain no tenant IDs, user IDs, device IDs, real organization data, or official Microsoft recommendations.

